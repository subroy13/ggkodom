% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kodom_heatmap.R
\name{kodom_heatmap}
\alias{kodom_heatmap}
\title{Heatmap trajectory plot}
\usage{
kodom_heatmap(
  data,
  id = "id",
  time = "time",
  value = "value",
  n_sample = 100,
  sort_by = "mean",
  time_bins = 10,
  time_breaks = NULL,
  agg_fun = "mean",
  colors = kodom_colors(),
  color_breaks = NULL,
  outcome = NULL,
  outcome_label = "*",
  facet_rows = NULL,
  facet_cols = NULL,
  seed = 123
)
}
\arguments{
\item{data}{Data frame in long format (one row per measurement).}

\item{id}{Column name (string) for subject identifier.}

\item{time}{Column name (string) for time variable (numeric).}

\item{value}{Column name (string) for measurement value (numeric).}

\item{n_sample}{Max subjects (default 100 — heatmaps tolerate
more rows than swimlanes).}

\item{sort_by}{Default \code{"mean"} (sensible for binned data).}

\item{time_bins}{Number of equal-width time bins (default 10).
Ignored if \code{time_breaks} is given.}

\item{time_breaks}{Numeric vector of custom bin boundaries.
Overrides \code{time_bins}.}

\item{agg_fun}{Aggregation within bins: \code{"mean"} (default),
\code{"median"}, \code{"first"}, \code{"last"}, \code{"min"},
or \code{"max"}.}

\item{colors}{Gradient colors; default \code{\link{kodom_colors}()}.}

\item{color_breaks}{Numeric breakpoints anchoring the gradient.
E.g.\ \code{c(5.7, 7, 10)} places the three default colors at
those values.  \code{NULL} spaces them evenly.}

\item{outcome}{Column name (string) for binary outcome.
Positive subjects get \code{outcome_label} on the y-axis.}

\item{outcome_label}{Marker for positive outcomes (default \code{"*"}).}

\item{facet_rows}{Column name for row faceting (\code{NULL} = none).}

\item{facet_cols}{Column name for column faceting (\code{NULL} = none).}

\item{seed}{Random seed for subject sampling.}
}
\value{
A \code{ggplot} object.
}
\description{
Subjects-by-time-bin grid where each cell is filled by the
aggregated measurement value.  Good for large \eqn{N} where
individual swimlane lines would overlap.
}
\examples{
set.seed(42)
df <- data.frame(
  id    = rep(1:30, each = 8),
  time  = rep(seq(0, 210, 30), 30),
  value = rnorm(240, 7, 1.5)
)
kodom_heatmap(df, n_sample = 30, time_bins = 7)
}
