% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kodom_circular.R
\name{kodom_circular}
\alias{kodom_circular}
\title{Circular trajectory plot — the Kodom}
\usage{
kodom_circular(
  data,
  id = "id",
  time = "time",
  value = "value",
  n_sample = 50,
  sort_by = "first",
  colors = kodom_colors(),
  color_breaks = NULL,
  outcome = NULL,
  outcome_label = "*",
  point_size = 2,
  line_width = 0.8,
  alpha = 0.7,
  gap_fraction = 0.15,
  inner_fraction = 0.3,
  direction = 1,
  show_rings = TRUE,
  seed = 123
)
}
\arguments{
\item{data}{Data frame in long format (one row per measurement).}

\item{id}{Column name (string) for subject identifier.}

\item{time}{Column name (string) for time variable (numeric).}

\item{value}{Column name (string) for measurement value (numeric).}

\item{n_sample}{Max number of subjects to display. \code{NULL} = all.}

\item{sort_by}{Order subjects by their \code{"first"} (default),
\code{"last"}, \code{"mean"}, or \code{"median"} value.}

\item{colors}{Gradient colors; default \code{\link{kodom_colors}()}.}

\item{color_breaks}{Numeric breakpoints anchoring the gradient.
E.g.\ \code{c(5.7, 7, 10)} places the three default colors at
those values.  \code{NULL} spaces them evenly.}

\item{outcome}{Column name (string) for binary outcome.
Positive subjects get \code{outcome_label} on the y-axis.}

\item{outcome_label}{Marker for positive outcomes (default \code{"*"}).}

\item{point_size}{Point size (default 3).}

\item{line_width}{Line width (default 0.8).}

\item{alpha}{Line transparency (default 0.7).}

\item{gap_fraction}{Fraction of the circle left empty as a visual
gap at the ordering seam (default 0.15).}

\item{inner_fraction}{Fraction of the time range used as a hollow
inner buffer so short-follow-up subjects remain visible (default 0.3).}

\item{direction}{\code{1} = clockwise (default), \code{-1} = counter-clockwise.}

\item{show_rings}{Draw concentric reference circles for time? (default TRUE).}

\item{seed}{Random seed for subject sampling.}
}
\value{
A \code{ggplot} object.
}
\description{
Polar variant of the swimlane: each subject is a radial spoke
emanating from the center.
Time increases outward (radius), color encodes measurement value.
The result resembles a Kadam flower (\emph{Neolamarckia cadamba}).
}
\details{
Subjects are arranged by \code{sort_by} going clockwise.
A configurable gap (\code{gap_fraction}) separates the lowest- and
highest-ordered subjects so the angular wrap is visible.
}
\examples{
set.seed(42)
n_subj <- 40
df <- data.frame(
  id    = rep(seq_len(n_subj), each = 5),
  time  = rep(seq(0, 120, 30), n_subj),
  value = rnorm(n_subj * 5, 7, 1.5)
)
kodom_circular(df, n_sample = 40)
}
