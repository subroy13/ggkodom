% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kodom_swimlane_multi.R
\name{kodom_swimlane_multi}
\alias{kodom_swimlane_multi}
\title{Multivariate functional swimlane}
\usage{
kodom_swimlane_multi(
  data,
  id = "id",
  time = "time",
  value = "value",
  variable = "variable",
  variables = NULL,
  anchor = NULL,
  sort_by = "first",
  colors_list = NULL,
  color_breaks_list = NULL,
  discretize = FALSE,
  n_sample = 50,
  point_size = 1.5,
  line_width = 0.5,
  alpha = 0.7,
  ncol = 1,
  seed = 123
)
}
\arguments{
\item{data}{Long-format data frame with one row per
(subject, variable, time).}

\item{id}{Column name for subject id (default \code{"id"}).}

\item{time}{Column name for time (default \code{"time"}).}

\item{value}{Column name for measurement value (default \code{"value"}).}

\item{variable}{Column name distinguishing variables
(default \code{"variable"}).}

\item{variables}{Character vector of variables to plot, in the desired
row order.  \code{NULL} (default) uses all variables in the data,
in their natural (factor / first-seen) order.}

\item{anchor}{Variable that determines the shared lane ordering.
\code{NULL} (default) = first entry of \code{variables}.}

\item{sort_by}{See \code{\link{kodom_swimlane}} — applied to the anchor
variable only.}

\item{colors_list}{Optional named list \code{list(varname = color_vec)}
to override per-variable color palettes.  Variables not named in the
list use \code{\link{kodom_colors}()}.}

\item{color_breaks_list}{Optional named list
\code{list(varname = numeric_breaks)} to set per-variable color
breakpoints (e.g. clinical thresholds: A1c at 7/8, BMI at 25/30).}

\item{discretize}{Either a single logical (applied to every panel) or
a named logical list (per-variable).}

\item{n_sample}{Max subjects (computed on the anchor variable; the same
set is then carried into every other panel).}

\item{point_size, line_width, alpha}{Cosmetic args, passed to every panel.}

\item{ncol}{Patchwork column count (default 1 = vertical stack).}

\item{seed}{Random seed for subject sampling on the anchor.}
}
\value{
A \pkg{patchwork} composite plot.
}
\description{
Stack one swimlane per variable, all sharing the same subject (lane)
ordering computed from a single anchor variable.  Each variable gets
its own color scale — A1c, BMI, LDL etc. live on different ranges and
should not be forced onto a common gradient.
}
\details{
This is the "show me all the trajectories at once" view for multivariate
longitudinal data.  Lane alignment across panels lets you read joint
dynamics by eye: the subject who deteriorates on A1c, does she also
gain weight?  Sort lanes by A1c (the anchor) and the BMI panel will
tell you immediately.

Requires the \pkg{patchwork} package.
}
\examples{
\dontrun{
  ## datathon-style: long with id / variable / time / value
  ## variables: a1c, bmi
  kodom_swimlane_multi(
    measurements,
    anchor = "a1c",
    color_breaks_list = list(a1c = c(6.5, 7, 8),
                             bmi = c(25, 30)),
    discretize = list(a1c = TRUE, bmi = FALSE)
  )
}
}
