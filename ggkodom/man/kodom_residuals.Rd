% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kodom_residuals.R
\name{kodom_residuals}
\alias{kodom_residuals}
\alias{kodom_residuals.lmerMod}
\alias{kodom_residuals.glmerMod}
\alias{kodom_residuals.gam}
\alias{kodom_residuals.fpca}
\alias{kodom_residuals.fpca.sc}
\alias{kodom_residuals.FPCA}
\title{Swimlane of residuals (observed - predicted)}
\usage{
kodom_residuals(fit, ...)

\method{kodom_residuals}{lmerMod}(fit, data = NULL, id = "id", time = "time", value = NULL, ...)

\method{kodom_residuals}{glmerMod}(fit, data = NULL, id = "id", time = "time", value = NULL, ...)

\method{kodom_residuals}{gam}(
  fit,
  data = NULL,
  id = "id",
  time = "time",
  value = NULL,
  type = "response",
  ...
)

\method{kodom_residuals}{fpca}(fit, data, id = "id", time = "time", value = "value", ...)

\method{kodom_residuals}{fpca.sc}(fit, ...)

\method{kodom_residuals}{FPCA}(fit, data, id = "id", time = "time", value = "value", ...)
}
\arguments{
\item{fit}{A fitted model object.}

\item{...}{Passed to method.}

\item{data}{Long-format data frame with the original observations
(\code{id}, \code{time}, \code{value}).  For lmer/gam, defaults to
the model frame stored in \code{fit}.}

\item{id, time, value}{Column names (strings).}

\item{signed_breaks}{Numeric vector of cut points for the divergent
color scale (default \code{c(-2, 0, 2)}).  Centered at zero.}

\item{colors}{Length-3 colors for (negative, zero, positive)
residuals.  Default = blue / grey / red.}

\item{discretize}{\code{FALSE} (default) = smooth divergent gradient;
\code{TRUE} = step bands at \code{signed_breaks}.}

\item{n_sample, sort_by, point_size, line_width, alpha, seed, outcome, outcome_label, facet_rows, facet_cols}{See \code{\link{kodom_swimlane}}.}
}
\value{
A \code{ggplot} object.
}
\description{
Predicts the supplied fit at each subject's actual observation times,
then plots a swimlane colored by the residual on a divergent palette
centered at zero.  The "what did the model miss" view.
}
\details{
Methods are provided for the same fit classes as \code{\link{kodom_fit}}:
\itemize{
\item \code{lme4::lmer}, \code{lme4::glmer}
\item \code{mgcv::gam}
\item \code{refund::fpca.sc}, \code{fdapace::FPCA}
}

For FPCA fits the reconstruction lives on \code{fit$workGrid} (or
\code{fit$argvals}); residuals at observed times are obtained by
linear interpolation per subject.
}
\examples{
\dontrun{
  library(lme4)
  m <- lmer(value ~ time + (1 + time | id), data = df)
  kodom_residuals(m, df)

  library(fdapace)
  Ly <- split(df$value, df$id); Lt <- split(df$time, df$id)
  fp <- FPCA(Ly = Ly, Lt = Lt, optns = list(dataType = "Sparse"))
  kodom_residuals(fp, df)
}
}
