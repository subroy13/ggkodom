% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_kodom.R
\docType{data}
\name{StatKodomPolar}
\alias{StatKodomPolar}
\alias{geom_kodom_polar}
\title{Polar (kodom flower) trajectory layer}
\usage{
geom_kodom_polar(
  mapping = NULL,
  data = NULL,
  stat = "kodom_polar",
  position = "identity",
  ...,
  n_sample = NULL,
  sort_by = "first",
  seed = 123,
  point_size = 2,
  gap_fraction = 0.15,
  inner_fraction = 0.3,
  direction = 1,
  arc_degrees = 360,
  arrow = NULL,
  lineend = "butt",
  linejoin = "round",
  linemitre = 10,
  na.rm = TRUE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2]{aes}}.}

\item{data}{A data frame.  If \code{NULL}, uses the plot data.}

\item{stat}{The statistical transformation to use.  Default
\code{"kodom"} applies subject sampling and sorting.}

\item{position}{Position adjustment.  Default \code{"identity"}.}

\item{...}{Other arguments passed to the layer: \code{linewidth},
\code{alpha}, \code{linetype}, etc.}

\item{n_sample}{Maximum number of subjects to display.  \code{NULL}
(default) shows all.}

\item{sort_by}{How to order subjects on the y axis.  One of
\code{"first"} (default), \code{"last"}, \code{"mean"},
\code{"median"} -- applies that summary to the \code{colour}
aesthetic per subject.  Or a named numeric vector whose names match
subject ids (e.g.\code{\link{kodom_sort_scores}}).}

\item{seed}{Random seed for subject sampling.}

\item{point_size}{Size of observation markers drawn on top of the path.
0 = no points.}

\item{gap_fraction}{Fraction of circle left empty at the ordering
seam (default 0.15).  Overridden when \code{arc_degrees < 360}.}

\item{inner_fraction}{Fraction of the time range used as a hollow
inner buffer (default 0.3).}

\item{direction}{\code{1} = clockwise (default), \code{-1} =
counter-clockwise.}

\item{arc_degrees}{Angular extent in degrees (default 360).  Use
e.g.\ 270 for a fan layout.}

\item{arrow}{Arrow specification for line ends (see
\code{\link[grid]{arrow}}).}

\item{lineend, linejoin, linemitre}{Line end/join style (passed to
\code{\link[ggplot2]{GeomPath}}).}

\item{na.rm}{Remove missing values silently?}

\item{show.legend}{Include this layer in legends?}

\item{inherit.aes}{Inherit aesthetics from the plot?}
}
\value{
A list containing a ggplot2 layer, \code{coord_polar},
position scales, and \code{theme_kodom_circular}.
}
\description{
A ggplot2 geom that arranges subjects as angular spokes in a polar
layout, with time as the radial axis.  The ggplot2-native equivalent
of \code{\link{kodom_swimlane}(coord = "polar")}.
}
\details{
Subjects occupy angular positions; time runs outward from the center.
A hollow inner region (\code{inner_fraction}) keeps short follow-up
subjects visible, and \code{gap_fraction} (or \code{arc_degrees})
leaves a visual gap at the ordering seam.

This function bundles a \code{coord_polar}, scales, and
\code{theme_kodom_circular()} so a basic call works out of the box.
You can override any of them with \code{+} after \code{geom_kodom_polar()}.
}
\section{Aesthetics}{

Same as \code{\link{geom_kodom}}: \code{x} (time), \code{y}
(subject), \code{colour} (value), \code{alpha}, \code{linewidth},
\code{linetype}.
}

\examples{
library(ggplot2)
set.seed(42)
df <- data.frame(
  id    = rep(1:20, each = 5),
  time  = rep(seq(0, 120, 30), 20),
  value = rnorm(100, 7, 1.5)
)

ggplot(df, aes(x = time, y = id, colour = value)) +
  geom_kodom_polar()

ggplot(df, aes(x = time, y = id, colour = value)) +
  geom_kodom_polar(arc_degrees = 270, n_sample = 15) +
  scale_color_kodom(color_breaks = c(5.7, 7, 10))
}
\keyword{datasets}
