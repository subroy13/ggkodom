% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kodom_multi.R
\name{kodom_multi}
\alias{kodom_multi}
\title{Polar multivariate kodom — concentric bands, one per variable}
\usage{
kodom_multi(
  data,
  id = "id",
  time = "time",
  value = "value",
  variable = "variable",
  variables = NULL,
  anchor = NULL,
  sort_by = "first",
  n_sample = 50,
  line_width = 0.5,
  alpha = 0.8,
  point_size = 1.2,
  arc_degrees = 360,
  gap_fraction = 0.15,
  inner_fraction = 0.3,
  band_gap_fraction = 0.1,
  direction = 1,
  show_band_labels = TRUE,
  show_band_rings = TRUE,
  seed = 123
)
}
\arguments{
\item{data}{Long-format data frame: one row per (subject, variable, time).}

\item{id, time, value, variable}{Column names (strings).}

\item{variables}{Character vector of variables to plot, in the desired
inside-out band order.  \code{NULL} (default) = all in natural order.}

\item{anchor}{Variable defining the lane (spoke) ordering.  \code{NULL}
(default) = first entry of \code{variables}.}

\item{sort_by}{See \code{\link{kodom_swimlane}} — applied to the anchor.}

\item{n_sample}{Max subjects (computed on the anchor).}

\item{line_width, alpha, point_size}{Cosmetics.}

\item{arc_degrees}{Angular extent in degrees (default 360).  See
\code{\link{kodom_swimlane}}.}

\item{gap_fraction}{Empty-arc fraction at the wrap seam (default 0.15);
overridden when \code{arc_degrees < 360}.}

\item{inner_fraction}{Hollow-center fraction (default 0.3).}

\item{band_gap_fraction}{Fraction of each band's radial width left as
a gap between bands (default 0.1).}

\item{direction}{\code{1} = clockwise (default), \code{-1} = ccw.}

\item{show_band_labels}{Print variable names at the seam, one per band
(default \code{TRUE}).}

\item{show_band_rings}{Faint dotted ring at each band boundary
(default \code{TRUE}).}

\item{seed}{Random seed for sampling.}
}
\value{
A \code{ggplot} object.
}
\description{
One spoke per subject (lane), split into \eqn{J} radial bands
(one per variable).  Time runs radially within each band.  Color
is the per-variable z-score, so all bands share a single gradient
even though A1c, BMI, LDL etc.\ live on completely different ranges.
}
\details{
This is the genuine multivariate extension of the kodom flower —
each "petal" gets striped by variable.  Compare to
\code{\link{kodom_swimlane_multi}}, which is the stacked / faceted
alternative (less visually integrated; better for raw-units reading).
}
\examples{
\dontrun{
  long <- rbind(
    transform(a1c_df, variable = "a1c"),
    transform(bmi_df, variable = "bmi")
  )
  kodom_multi(long, anchor = "a1c")
  kodom_multi(long, anchor = "a1c", arc_degrees = 270)
}
}
