% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kodom_scores.R
\name{kodom_scores}
\alias{kodom_scores}
\alias{kodom_scores.fpca}
\alias{kodom_scores.fpca.sc}
\alias{kodom_scores.FPCA}
\title{Scatter plot of FPC scores}
\usage{
kodom_scores(fit, ...)

\method{kodom_scores}{fpca}(
  fit,
  pcs = c(1, 2),
  groups = NULL,
  ids = NULL,
  point_size = 2.5,
  alpha = 0.85,
  show_pve = TRUE,
  colors = NULL,
  ...
)

\method{kodom_scores}{fpca.sc}(fit, ...)

\method{kodom_scores}{FPCA}(
  fit,
  pcs = c(1, 2),
  groups = NULL,
  ids = NULL,
  point_size = 2.5,
  alpha = 0.85,
  show_pve = TRUE,
  colors = NULL,
  ...
)
}
\arguments{
\item{fit}{An FPCA fit (\code{refund::fpca.sc} or
\code{fdapace::FPCA}).}

\item{...}{Unused.}

\item{pcs}{Length-2 integer vector — which PCs go on the x and y axes
(default \code{c(1, 2)}).}

\item{groups}{Optional vector (length = number of subjects).
Numeric → kodom continuous gradient; factor/character → discrete
palette.}

\item{ids}{Subject ids in row order of the score matrix.
Defaults: \code{rownames(fit$scores)} for refund;
\code{names(fit$inputData$Ly)} for fdapace; integer index otherwise.}

\item{point_size}{Point size (default 2.5).}

\item{alpha}{Point transparency (default 0.85).}

\item{show_pve}{Annotate axes with proportion of variance explained.}

\item{colors}{Optional colors (gradient anchors for numeric groups,
one per level for discrete).}
}
\value{
A \code{ggplot} object.
}
\description{
Visualize subjects in the score space of an FPCA fit
(\eqn{\xi_1, \xi_2, \ldots}).  Useful for spotting clusters,
phenotypes, or outliers in the functional decomposition.
}
\examples{
\dontrun{
  library(fdapace)
  Ly <- split(df$value, df$id); Lt <- split(df$time, df$id)
  fp <- FPCA(Ly = Ly, Lt = Lt, optns = list(dataType = "Sparse"))
  kodom_scores(fp)
  kodom_scores(fp, groups = sapply(Ly, mean))   # color by mean A1c
}
}
