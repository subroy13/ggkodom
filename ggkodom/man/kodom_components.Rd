% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kodom_components.R
\name{kodom_components}
\alias{kodom_components}
\alias{kodom_components.fpca}
\alias{kodom_components.fpca.sc}
\alias{kodom_components.FPCA}
\title{Plot the eigenfunctions of an FPCA fit}
\usage{
kodom_components(fit, ...)

\method{kodom_components}{fpca}(fit, ...)

\method{kodom_components}{fpca.sc}(fit, ...)

\method{kodom_components}{FPCA}(
  fit,
  K = NULL,
  overlay = FALSE,
  show_pve = TRUE,
  colors = NULL,
  line_width = 1,
  show_mean = FALSE,
  boot_fits = NULL,
  boot_alpha = 0.15,
  ...
)
}
\arguments{
\item{fit}{An FPCA fit object.}

\item{...}{Passed to method.}

\item{K}{Number of components to plot.  \code{NULL} = all retained.}

\item{overlay}{\code{FALSE} (default) = one panel per PC, stacked
vertically; \code{TRUE} = all PCs overlaid in a single panel.}

\item{show_pve}{Annotate each PC with its proportion of variance
explained (default \code{TRUE}).}

\item{colors}{Color vector for the PCs.  Default = ramped
\code{\link{kodom_colors}()} of length \code{K}.}

\item{line_width}{Line width (default 1).}

\item{show_mean}{Add a panel for the mean function \eqn{\mu(t)} at the
top (default \code{FALSE}).  The mean isn't an eigenfunction but is
often shown alongside for context.}

\item{boot_fits}{Optional list of bootstrap FPCA fits (e.g.\ from
\code{\link{fpca_boot_subjects}}).  Each fit's eigenfunctions are
sign-aligned to the main fit and drawn as light traces behind the
main curve, giving a visual variability envelope.}

\item{boot_alpha}{Transparency for bootstrap traces (default 0.15).}
}
\value{
A \code{ggplot} object.
}
\description{
Each functional principal component is itself a curve
\eqn{\phi_k(t)}.  This generic visualizes the first \code{K} of them
as a small-multiples panel (default) or as overlaid lines.
}
\details{
Methods are provided for:
\itemize{
\item \code{refund::fpca.sc} (class \code{"fpca"} or \code{"fpca.sc"})
\item \code{fdapace::FPCA}   (class \code{"FPCA"})
}
}
\examples{
\dontrun{
  library(fdapace)
  Ly <- split(df$value, df$id); Lt <- split(df$time, df$id)
  fp <- FPCA(Ly = Ly, Lt = Lt, optns = list(dataType = "Sparse"))
  kodom_components(fp)                          # 3 stacked panels
  kodom_components(fp, K = 2, overlay = TRUE)   # overlaid
  kodom_components(fp, show_mean = TRUE)        # with mu(t) panel
}
}
