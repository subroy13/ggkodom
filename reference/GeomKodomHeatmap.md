# Geom for geom_kodom_heatmap

Extends
[ggplot2::GeomTile](https://ggplot2.tidyverse.org/reference/Geom.html)
with kodom defaults. Drops `id` in `setup_data()` to avoid an
unknown-aesthetic warning from ggplot2.

## Usage

``` r
GeomKodomHeatmap
```

## Format

A ggproto object.
