# Stat for geom_kodom_line

Computes integer lane positions (`y`) from the `id` aesthetic and
applies optional sorting and subject sampling. Called automatically by
[`geom_kodom_line()`](geom_kodom_line.md); rarely needed directly.

## Usage

``` r
StatKodomLine
```

## Format

A ggproto object.
