# Base stat shared by all kodom geoms

Declares the `x` and `id` required aesthetics. Concrete stats inherit
from this and override `compute_panel()`.

## Usage

``` r
StatKodomBase
```

## Format

A ggproto object.
