% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_kodom_circular.R
\name{geom_kodom_circular}
\alias{geom_kodom_circular}
\title{Circular ("Kodom") swimlane plot for longitudinal trajectories}
\usage{
geom_kodom_circular(
  mapping = NULL,
  data = NULL,
  stat = StatKodomCircular,
  position = "identity",
  ...,
  sort_by = "none",
  n_max = Inf,
  gap_fraction = 0.15,
  inner_fraction = 0.3,
  direction = 1L,
  show_points = TRUE,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.}

\item{data}{A data frame. If \code{NULL}, inherits from the plot.}

\item{stat}{The stat to use. Defaults to \link{StatKodomCircular}.}

\item{position}{Position adjustment, almost always \code{"identity"}.}

\item{...}{Other arguments passed to \code{\link[ggplot2:layer]{ggplot2::layer()}}.}

\item{sort_by}{Lane ordering (angular order). One of \code{"none"} (default),
\code{"mean"}, \code{"mean_asc"}, \code{"first"}, \code{"last"}.}

\item{n_max}{Maximum number of subjects to display. Default \code{Inf}.}

\item{gap_fraction}{Fraction of the full circle left empty as a visual
gap at the ordering seam. Default \code{0.15}.}

\item{inner_fraction}{Fraction of the time range used as a hollow inner
buffer so short-follow-up subjects remain visible. Default \code{0.3}.}

\item{direction}{\code{1L} = clockwise (default), \code{-1L} = counter-clockwise.}

\item{show_points}{If \code{TRUE} (default), draws a point at every
observation. Set to \code{FALSE}, or map \code{shape = NA} / \code{size = 0}, to
suppress points.}

\item{na.rm}{If \code{TRUE}, silently remove rows with missing required
aesthetics.}

\item{show.legend}{Logical. Should this layer appear in the legend?}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics.}
}
\value{
A ggplot2 layer object.
}
\description{
Draws a radial plot where each subject occupies an angular spoke and
time increases outward from the centre. Measurements are encoded as a
colour gradient along each radial path, interpolated between consecutive
observations. The layout resembles a Kadam flower
(\emph{Neolamarckia cadamba}), giving the package its name.
}
\details{
Unlike the original \code{coord_polar()} variant in the archive package,
this geom performs the polar-to-Cartesian transform inside the stat and
renders in ordinary Cartesian space, so all ggplot2 scales, themes, and
facets work normally. Use \code{coord_fixed()} to preserve the circular shape.
}
\section{Aesthetics}{

Required aesthetics are shown in \strong{bold}. \code{size} and \code{linewidth} are
intentionally independent: each controls a different graphical element.
\itemize{
\item \strong{\code{x}} — time variable (numeric); mapped to radial distance
\item \strong{\code{id}} — subject identifier; mapped to angular position
\item \code{colour} — color of both the connecting path and the point border
\item \code{fill} — point interior color for filled shapes (21–25)
\item \code{size} — \strong{point size only}; set to \code{NA} or \code{0} to suppress points
\item \code{linewidth} — \strong{path width only}
\item \code{alpha} — transparency applied to both path and points
\item \code{shape} — point shape (default 19). Set to \code{NA} to suppress points.
\item \code{stroke}, \code{linetype} — point border width / path line type
}
}

\examples{
\dontrun{
ggplot(df, aes(x = time, id = subject_id, colour = hba1c)) +
  geom_kodom_circular(sort_by = "mean", gap_fraction = 0.1) +
  scale_colour_kodom() +
  coord_fixed() +
  theme_kodom_circular()
}
}
