% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colors.R
\name{scale_fill_kodom}
\alias{scale_fill_kodom}
\title{Kodom fill scale for the fill aesthetic}
\usage{
scale_fill_kodom(
  colors = kodom_colors(),
  color_breaks = NULL,
  name = ggplot2::waiver(),
  discretize = FALSE,
  ...
)
}
\arguments{
\item{colors}{Color vector. Defaults to \code{\link[=kodom_colors]{kodom_colors()}}.}

\item{color_breaks}{Numeric breakpoints anchoring the gradient via
\code{\link[scales:rescale]{scales::rescale()}}. For \code{discretize = TRUE}, each break adds one band:
\code{k} breaks produce \code{k + 1} bands. \code{NULL} = evenly spaced.}

\item{name}{Legend title. Defaults to \code{"value"}.}

\item{discretize}{\code{FALSE} (default) = smooth gradient; \code{TRUE} = step bands
at \code{color_breaks}.}

\item{...}{Ignored.}
}
\value{
A ggplot2 scale object.
}
\description{
Identical behavior to \code{\link[=scale_color_kodom]{scale_color_kodom()}} but targets the \code{fill}
aesthetic. Intended for use with \code{\link[=geom_kodom_heatmap]{geom_kodom_heatmap()}}.
}
\examples{
\dontrun{
ggplot(df, aes(x = time, id = subject_id, fill = value)) +
  geom_kodom_heatmap() +
  scale_fill_kodom()
}
}
