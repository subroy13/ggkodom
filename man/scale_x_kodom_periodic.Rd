% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_kodom.R
\name{scale_x_kodom_periodic}
\alias{scale_x_kodom_periodic}
\title{X-scale for periodic longitudinal plots}
\usage{
scale_x_kodom_periodic(period = 12, breaks = 1:period, ...)
}
\arguments{
\item{period}{The length of one complete cycle (e.g. \code{12} for months).
Must match the \code{period} argument passed to \code{geom_kodom_periodic()}.
Default \code{12}.}

\item{breaks}{Passed to \code{scale_x_continuous()}. Default provides integer
breaks for the period.}

\item{...}{Additional arguments passed to \code{\link[ggplot2:scale_continuous]{ggplot2::scale_x_continuous()}},
such as \code{labels}.}
}
\value{
A \code{\link[ggplot2:scale_continuous]{ggplot2::scale_x_continuous()}} object.
}
\description{
This is a convenience wrapper around \code{\link[ggplot2:scale_continuous]{ggplot2::scale_x_continuous()}} that
enforces \code{limits = c(0, period)} and uses \code{oob = scales::oob_keep}.
}
\details{
\strong{Why is this necessary?} To make exactly one cycle span exactly one
360-degree rotation in \code{coord_polar}, the scale limits \emph{must} be set to the
period length (e.g., \code{c(0, 12)}). However, standard ggplot2 \code{scale_x_continuous}
will drop any data outside those limits. By setting \code{oob = scales::oob_keep},
we instruct ggplot2 to keep the data that exceeds the period. \code{coord_polar}
then natively wraps those out-of-bounds values around the circle, creating
beautiful, continuous Archimedean spirals!
}
\examples{
\dontrun{
ggplot(df, aes(x = visit_month, id = subject_id, colour = hba1c)) +
  geom_kodom_periodic(period = 12) +
  scale_x_kodom_periodic(period = 12, labels = month.abb) +
  scale_y_kodom_periodic() +
  coord_kodom_periodic() +
  theme_kodom_periodic()
}
}
