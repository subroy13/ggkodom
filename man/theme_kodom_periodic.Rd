% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_kodom.R
\name{theme_kodom_periodic}
\alias{theme_kodom_periodic}
\title{Theme for periodic Kodom plots}
\usage{
theme_kodom_periodic(base_size = 12)
}
\arguments{
\item{base_size}{Base font size (default 12).}
}
\value{
A \code{\link[ggplot2:theme]{ggplot2::theme()}} object.
}
\description{
Designed for use with \code{\link[=geom_kodom_periodic]{geom_kodom_periodic()}} and
\code{\link[=coord_kodom_periodic]{coord_kodom_periodic()}}. Suppresses the radial (y) axis — which shows
lane numbers that are not meaningful to readers — while keeping the angular
(x) axis so that time labels added via \code{\link[ggplot2:scale_continuous]{ggplot2::scale_x_continuous()}} are
visible around the ring.
}
\examples{
\dontrun{
ggplot(df, aes(x = visit_month, id = subject_id, colour = hba1c)) +
  geom_kodom_periodic(period = 12) +
  scale_x_continuous(breaks = 1:12, labels = month.abb, limits = c(0, 12)) +
  scale_colour_kodom() +
  coord_kodom_periodic() +
  theme_kodom_periodic()
}
}
