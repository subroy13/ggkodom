% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/theme_kodom.R
\name{theme_kodom_circular}
\alias{theme_kodom_circular}
\title{Theme for circular ggkodom plots}
\usage{
theme_kodom_circular(base_size = 12)
}
\arguments{
\item{base_size}{Base font size (default 12).}
}
\value{
A \code{\link[ggplot2:theme]{ggplot2::theme()}} object.
}
\description{
Fully minimal Cartesian theme for \code{\link[=geom_kodom_circular]{geom_kodom_circular()}}: no axis text,
ticks, or grid lines. Reference rings are drawn by the geom itself as
annotation layers, so this theme suppresses all panel grid elements.
}
\details{
Unlike the original \code{coord_polar()}-based approach, \code{\link[=geom_kodom_circular]{geom_kodom_circular()}}
performs its coordinate transformation inside the stat and renders in
ordinary Cartesian space. This means \code{panel.grid.major.y} lines would
appear as horizontal rules, not concentric circles — so both major axes
are suppressed here.
}
\examples{
\dontrun{
ggplot(df, aes(x = time, id = subject_id, colour = value)) +
  geom_kodom_circular() +
  scale_colour_kodom() +
  coord_fixed() +
  theme_kodom_circular()
}
}
