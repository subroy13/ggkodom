% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_kodom_heatmap.R
\name{geom_kodom_heatmap}
\alias{geom_kodom_heatmap}
\title{Swimlane heatmap for longitudinal trajectories}
\usage{
geom_kodom_heatmap(
  mapping = NULL,
  data = NULL,
  stat = StatKodomHeatmap,
  position = "identity",
  ...,
  sort_by = "mean",
  n_max = Inf,
  bins = 10L,
  breaks = NULL,
  fun = "mean",
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.}

\item{data}{A data frame. If \code{NULL}, inherits from the plot.}

\item{stat}{The stat to use. Defaults to \link{StatKodomHeatmap}.}

\item{position}{Position adjustment, almost always \code{"identity"}.}

\item{...}{Other arguments passed to \code{\link[ggplot2:layer]{ggplot2::layer()}}.}

\item{sort_by}{Lane ordering. One of \code{"none"}, \code{"mean"} (default),
\code{"mean_asc"}, \code{"first"}, \code{"last"} — all refer to the \code{fill} variable.}

\item{n_max}{Maximum number of subjects to display. A random sample is
taken when exceeded. Default \code{Inf} (all subjects).}

\item{bins}{Number of equal-width time bins. Default \code{10L}. Ignored if
\code{breaks} is supplied.}

\item{breaks}{Numeric vector of explicit bin boundaries. Overrides \code{bins}.}

\item{fun}{Aggregation function per cell. One of \code{"mean"} (default),
\code{"median"}, \code{"first"}, \code{"last"}, \code{"min"}, \code{"max"}.}

\item{na.rm}{If \code{TRUE}, silently remove rows with missing required
aesthetics.}

\item{show.legend}{Logical. Should this layer appear in the legend?}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics.}
}
\value{
A ggplot2 layer object.
}
\description{
Divides the time axis into equal-width bins and fills each
(subject × bin) cell with an aggregate of the measured value. Each
subject occupies one horizontal lane. Use this layout when the cohort
is large or when you prefer a compact, aligned grid over individual
paths.
}
\details{
Lane ordering is controlled by \code{sort_by} (defaults to \code{"mean"}, which
places the highest-mean subjects at the top). Time bins can be
customised via \code{bins} (number of equal-width intervals) or \code{breaks}
(explicit boundaries).
}
\section{Aesthetics}{

Required aesthetics are shown in \strong{bold}.
\itemize{
\item \strong{\code{x}} — time variable (numeric or Date)
\item \strong{\code{id}} — subject identifier; determines lane position on the y axis
\item \strong{\code{fill}} — measurement value used for cell color and sorting
\item \code{colour} — tile border color (default \code{"white"})
\item \code{linewidth} — tile border width (default \code{0.25})
\item \code{alpha} — transparency
}
}

\examples{
\donttest{
library(ggplot2)
df <- data.frame(
  subject_id = rep(1:5, each = 4),
  time = rep(1:4, 5),
  visit_month = rep(1:4, 5),
  value = rep(1:4, 5),
  hba1c = rep(1:4, 5),
  arm = rep(c("Treatment", "Control"), c(12, 8))
)
ggplot(df, aes(x = time, id = subject_id, fill = hba1c)) +
  geom_kodom_heatmap(sort_by = "mean", bins = 12) +
  scale_fill_kodom() +
  theme_kodom()
}
}
