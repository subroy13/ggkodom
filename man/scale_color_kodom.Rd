% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/colors.R
\name{scale_color_kodom}
\alias{scale_color_kodom}
\alias{scale_colour_kodom}
\title{Kodom color scale for the color aesthetic}
\usage{
scale_color_kodom(
  colors = kodom_colors(),
  color_breaks = NULL,
  name = ggplot2::waiver(),
  discretize = FALSE,
  ...
)

scale_colour_kodom(
  colors = kodom_colors(),
  color_breaks = NULL,
  name = ggplot2::waiver(),
  discretize = FALSE,
  ...
)
}
\arguments{
\item{colors}{Color vector. Defaults to \code{\link[=kodom_colors]{kodom_colors()}}.}

\item{color_breaks}{Numeric breakpoints anchoring the gradient via
\code{\link[scales:rescale]{scales::rescale()}}. For \code{discretize = TRUE}, each break adds one band:
\code{k} breaks produce \code{k + 1} bands. \code{NULL} = evenly spaced.}

\item{name}{Legend title. Defaults to \code{"value"}.}

\item{discretize}{\code{FALSE} (default) = smooth gradient; \code{TRUE} = step bands
at \code{color_breaks}.}

\item{...}{Ignored.}
}
\value{
A ggplot2 scale object.
}
\description{
A ggplot2 color scale using the Kadam flower palette (teal -> gold -> red).
Designed to compose with \code{\link[=geom_kodom_line]{geom_kodom_line()}}, \code{\link[=geom_kodom_heatmap]{geom_kodom_heatmap()}}, and
\code{\link[=geom_kodom_circular]{geom_kodom_circular()}}.
}
\details{
Set \code{discretize = TRUE} to switch from a smooth gradient to solid color
bands separated at \code{color_breaks} — useful when values change slowly and
a continuous gradient washes out into a single hue.
}
\examples{
\dontrun{
ggplot(df, aes(x = time, id = subject_id, color = value)) +
  geom_kodom_line() +
  scale_color_kodom()

# Discrete bands at clinical thresholds
ggplot(df, aes(x = time, id = subject_id, color = hba1c)) +
  geom_kodom_line() +
  scale_color_kodom(discretize = TRUE, color_breaks = c(5.7, 6.5))
}
}
