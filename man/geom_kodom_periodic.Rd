% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_kodom_periodic.R
\name{geom_kodom_periodic}
\alias{geom_kodom_periodic}
\title{Periodic "Star Trail" swimlane plot for longitudinal trajectories}
\usage{
geom_kodom_periodic(
  mapping = NULL,
  data = NULL,
  stat = StatKodomPeriodic,
  position = "identity",
  ...,
  period = 12,
  spiral_fraction = 0.1,
  inner_fraction = 0.3,
  lane_width = 1,
  sort_by = "none",
  n_max = Inf,
  show_points = TRUE,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.}

\item{data}{A data frame. If \code{NULL}, inherits from the plot.}

\item{stat}{The stat to use. Defaults to \link{StatKodomLine}.}

\item{position}{Position adjustment, almost always \code{"identity"}.}

\item{...}{Other arguments passed to \code{\link[ggplot2:layer]{ggplot2::layer()}}.}

\item{period}{Numeric. Length of one complete cycle (e.g. 12 for months,
24 for hours). Default \code{12}.}

\item{spiral_fraction}{Numeric. Radial expansion per full cycle as a
fraction of one \emph{original} lane width (i.e. before \code{lane_width} scaling).
\code{0} keeps cycles on the same ring; \code{0.1} (default) drifts outward by
10\% of one lane per cycle.}

\item{inner_fraction}{Fraction of total lanes used as a hollow inner buffer.
Default \code{0.3}. Pair with \code{\link[=scale_y_kodom_periodic]{scale_y_kodom_periodic()}} to make the hole
visible.}

\item{lane_width}{Positive numeric. Multiplier for the radial distance
between adjacent subject rings. Default \code{1} packs rings at unit spacing.
Increase (e.g. \code{3}) to spread rings apart so each subject's arc is more
legible — particularly useful when using \code{n_max} to show a small subset.
The hollow center (\code{inner_fraction}) is intentionally not scaled so the
hole size stays proportional to the cohort, not the spacing.}

\item{sort_by}{Lane ordering. One of \code{"none"} (default), \code{"mean"},
\code{"mean_asc"}, \code{"first"}, \code{"last"} — all refer to the \code{colour} variable.}

\item{n_max}{Maximum number of subjects to display. A random sample is taken
when exceeded. Default \code{Inf} (all subjects).}

\item{show_points}{If \code{TRUE} (default), draws a point at every observation.
Set to \code{FALSE}, or map \code{shape = NA} / \code{size = 0}, to suppress points.}

\item{na.rm}{If \code{TRUE}, silently remove rows with missing required aesthetics.}

\item{show.legend}{Logical. Should this layer appear in the legend?}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics.}
}
\value{
A ggplot2 layer object.
}
\description{
Draws concentric arcs representing cyclical longitudinal data. Time maps to
angle (one full revolution equals one \code{period}), and each subject's base
radius is determined by its lane rank. To separate successive cycles of the
same subject the radius drifts outward slightly as total time increases,
forming a star trail or Archimedean spiral.
}
\details{
\strong{Coordinate system.} This geom is designed for use with
\code{coord_polar(theta = "x")}. Use \code{\link[=coord_kodom_periodic]{coord_kodom_periodic()}} as a convenient
wrapper that sets \code{start = pi/2} (12 o'clock at \code{x = 0}) and
\code{direction = -1} (clockwise) by default.

\strong{Axis labels.} Because \code{x} is continuous time, \code{ggplot2} will automatically
expand the limits to cover the entire time range if you use standard scales,
which alters the length of a full 360-degree rotation. To make one rotation
exactly equal one \code{period}, use the included wrapper \code{\link[=scale_x_kodom_periodic]{scale_x_kodom_periodic()}}:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{scale_x_kodom_periodic(period = 12, breaks = 1:12, labels = month.abb)
}\if{html}{\out{</div>}}
}
