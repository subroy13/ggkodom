% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/geom_kodom_line.R
\name{geom_kodom_line}
\alias{geom_kodom_line}
\title{Swimlane line plot for longitudinal trajectories}
\usage{
geom_kodom_line(
  mapping = NULL,
  data = NULL,
  stat = StatKodomLine,
  position = "identity",
  ...,
  sort_by = "none",
  n_max = Inf,
  show_points = TRUE,
  na.rm = FALSE,
  show.legend = NA,
  inherit.aes = TRUE
)
}
\arguments{
\item{mapping}{Set of aesthetic mappings created by \code{\link[ggplot2:aes]{ggplot2::aes()}}.}

\item{data}{A data frame. If \code{NULL}, inherits from the plot.}

\item{stat}{The stat to use. Defaults to \link{StatKodomLine}.}

\item{position}{Position adjustment, almost always \code{"identity"}.}

\item{...}{Other arguments passed to \code{\link[ggplot2:layer]{ggplot2::layer()}}.}

\item{sort_by}{Lane ordering. One of \code{"none"} (default), \code{"mean"},
\code{"mean_asc"}, \code{"first"}, \code{"last"} — all refer to the \code{colour} variable.}

\item{n_max}{Maximum number of subjects to display. A random sample is taken
when exceeded. Default \code{Inf} (all subjects).}

\item{show_points}{If \code{TRUE} (default), draws a point at every observation.
Set to \code{FALSE}, or map \code{shape = NA} / \code{size = 0}, to suppress points.}

\item{na.rm}{If \code{TRUE}, silently remove rows with missing required aesthetics.}

\item{show.legend}{Logical. Should this layer appear in the legend?}

\item{inherit.aes}{If \code{FALSE}, overrides the default aesthetics.}
}
\value{
A ggplot2 layer object.
}
\description{
Draws one horizontal colored path per subject. Time maps to \code{x}, subject
identity maps to \code{id} (which the stat converts to integer lane positions on
the \code{y} axis), and a measured value maps to \code{colour}. The colour transitions
smoothly between consecutive observations via interpolated sub-segments.
}
\details{
Lane ordering is controlled by \code{sort_by}. With many subjects, consider
suppressing y-axis labels via \code{theme(axis.text.y = element_blank())}.
}
\section{Aesthetics}{

Required aesthetics are shown in \strong{bold}. \code{size} and \code{linewidth} are
intentionally independent: each controls a different graphical element.
\itemize{
\item \strong{\code{x}} — time variable (numeric or Date)
\item \strong{\code{id}} — subject identifier; determines lane position on the y axis
\item \code{colour} — color of both the connecting path and the point border
\item \code{fill} — point interior color for filled shapes (21–25); no effect on path
\item \code{size} — \strong{point size only}; set to \code{NA} or \code{0} to suppress points
\item \code{linewidth} — \strong{path width only}; does not affect observation points
\item \code{alpha} — transparency applied to both path and points
\item \code{shape} — point shape (default 19). Set to \code{NA} to suppress points.
\item \code{stroke}, \code{linetype} — point border width / path line type
}
}

\examples{
\donttest{
library(ggplot2)
df <- data.frame(
  subject_id = rep(1:5, each = 4),
  time = rep(1:4, 5),
  visit_month = rep(1:4, 5),
  value = rep(1:4, 5),
  hba1c = rep(1:4, 5),
  arm = rep(c("Treatment", "Control"), c(12, 8))
)
ggplot(df, aes(x = time, id = subject_id, colour = hba1c)) +
  geom_kodom_line(sort_by = "mean", n_max = 50) +
  scale_colour_kodom() +
  theme_kodom()
}
}
